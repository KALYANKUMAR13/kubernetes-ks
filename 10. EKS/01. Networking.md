
In  kube-system ns, will have 2Pods- coredns for service discovery, 2 pods for kube-proxy(networking), and 2 aws-node pods. All these are daemon set

<img width="884" height="488" alt="image" src="https://github.com/user-attachments/assets/de7559f4-5284-4017-87a7-b316a5f0104d" />

AWS VPC CNI, based on instacnce size only it can have seconday IPs for pods and Number of ENI can be attahced, once the ip is comleteded , need to have another eni attached to a node, then we can use secondary IPs for pods
<img width="884" height="479" alt="image" src="https://github.com/user-attachments/assets/feec55a4-c087-41b0-8a49-9b1bb9b57b25" />


https://docs.aws.amazon.com/eks/latest/best-practices/vpc-cni.html


Attaching ENI to a node is not instaneous, it is 15seconds to 15minsutes, it varies. (Depends on how many IPs availiable in subnets)

<img width="935" height="485" alt="image" src="https://github.com/user-attachments/assets/1f533f18-f6bb-4fc1-8c0b-191220c38fe3" />


For the ENi attached, we can set warm eni target. which means, I have 
For an c6a.large instance, the limits are:

Maximum network interfaces (ENIs)	3
IPv4 addresses per ENI	10 (including the primary IP)
Total IPv4 addresses	30

Inititally 1 eni is attached to a node, if we set WARM_ENI_TARGET, node will get another ENI ready so that we can get pods running asap without waiting for eni provision and its time.
<img width="907" height="491" alt="image" src="https://github.com/user-attachments/assets/5710ab0b-3743-40f7-a86b-a3771852c24b" />


Depends on the instance size, number of  enis and number of secodary IPs are deceided. 
WARM_IP_TARGET(for IPs need to be reserved)
To solve above issue regarding the pod ips ->Recommeded way ENABLE_PREFIX_DELEGLATION


<img width="943" height="388" alt="image" src="https://github.com/user-attachments/assets/9f4269b9-9c37-4e44-8cc7-9f8c365ceead" />


Each ENI will have a CIDR and routing table(aws takes care, so each eni will have more number of IP when compared to normal secondary IPs)

Probelm with PREFIX_DELEGLATION is Lag of IPs in the subnet, If subnet doesn't have IPs, it will be a problem 
<img width="840" height="487" alt="image" src="https://github.com/user-attachments/assets/5beaa370-467f-43f9-9379-e769dfc28f88" />
